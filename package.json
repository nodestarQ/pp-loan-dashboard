{
  "name": "pp-loan-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Pudgy Penguins Huddle Health dashboard",
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "dev:web": "pnpm --filter @pp/web dev",
    "dev:indexer": "pnpm --filter @pp/indexer dev",
    "dev:stack": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d postgres indexer",
    "dev:stack:down": "docker compose down",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "up": "docker compose up",
    "down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}
